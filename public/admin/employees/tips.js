// SR&ED-specific terminology that admins repeatedly bump into; hover help
// keeps the labels short while still defining the term where it's used. Plain
// `title=` attribute — good enough for desktop, accessible to screen readers,
// and zero CSS/JS cost.
//
// Shared across the list-view forms (Add employee, Attach existing) and the
// inline edit-user expansion (Add attachment, per-attachment Add comp row).
// Kept in one place so a wording tweak lands in every form simultaneously.

export const TIP_SPECIFIED = 'Hourly rate is capped at the per-year specified-employee cap on T661 line 307. Flag for owners, partners, and people earning above the cap.';
export const TIP_COMP_TYPE = 'Salary = annual; hourly = per-hour. The amount input flips to match.';
